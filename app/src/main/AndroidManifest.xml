<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fpoly.dell.project1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.ManHinhChaoActivity"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.HomeActivity" />
        <activity
            android:name=".ui.ChungLoaiActivity"
            android:parentActivityName=".ui.HomeActivity" />
        <activity
            android:name=".ui.VatNuoiActivity"
            android:parentActivityName=".ui.HomeActivity" />
        <activity
            android:name=".ui.ChiTietChungLoaiActivity"
            android:parentActivityName=".ui.ChungLoaiActivity" />
        <activity
            android:name=".ui.ThemChungLoaiActivity"
            android:parentActivityName=".ui.ChungLoaiActivity" />
        <activity
            android:name=".ui.ThemVatNuoiActivity"
            android:parentActivityName=".ui.VatNuoiActivity" />
        <activity android:name=".ui.KinhNghiemActivity"
            android:parentActivityName=".ui.HomeActivity" ></activity>
    </application>

</manifest>